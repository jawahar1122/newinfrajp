trigger:
  - main

pool: TKS-AGENT

stages:
  - stage: "Terraform"
    jobs:
    - job: "terraforminstall"
      steps:
      - task: TerraformInstaller@1
        inputs:
          terraformVersion: 'latest'